---
title: Email summary of validated data 
output: blastula::blastula_email
---

## `r glue("🌭 {num_inspections} inspection records validated 🌮")`

A sample of the data is below:

```{r, echo=FALSE}

inspections_validated |> select(-violations) |>
  slice_sample(n=20) |>  gt::gt() |> 
  gt::tab_header(
    title = md(glue("**Sample of Inspections Data, most recent date: {last_inspection_date}**")),
    subtitle = md("Inspections data for Restaurant, Grocery Store, Coffee Shop, Bakery, or Wrigley Field Rooftop ⚾")) |>
  gt::tab_source_note(source_note = md(glue("**Summary of records: {num_last_30day} in last 30 days <br>
                                        Last 30 day fail rate: {round(num_fail_last_30day/num_last_30day*100,1)}% <br>
                                        Total inspection records: {num_inspections}**"))) 
  
```
